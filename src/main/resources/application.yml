spring:
  application:
    name: MyBlog

  datasource:
    url: jdbc:mysql://localhost:3306/blog_db
    username: root
    password: root12
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true

server:
  port: 80
  servlet:
    session:
      timeout: 30m

